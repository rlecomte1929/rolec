{
  "name": "relopass-v2-campaign",
  "version": "2.0.0",
  "private": true,
  "description": "ReloPass Testing Master Document v2.0 — autonomous Playwright campaign against production (relopass.com). Auth once per persona via storageState; specs reuse the saved sessions.",
  "scripts": {
    "auth": "playwright test --project=setup",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
