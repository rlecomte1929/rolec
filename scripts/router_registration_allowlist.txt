# router_registration_allowlist.txt
# Grandfathered modular-only routers — registered in backend/app/main.py but NOT
# in backend/main.py (the prod entrypoint). Render boots `uvicorn backend.main:app`
# and does NOT serve the modular app, so a router registered only in app/main.py
# returns 405 in production (the recurring CLAUDE.md footgun). The guard
# (scripts/check_router_registrations.py) fails on any NEW modular-only router not
# listed here. One identity per line; '#' comments ignored.
#
# This list should DRAIN over time, not grow.
#
# Seeded 2026-06-01 from main @ a19291d7 with 21 entries. Drained 2026-06-03: 18 of
# those 21 are now registered in backend/main.py (PR #235 restored 17 routers plus
# follow-ups — advisors, marketplace, hr_analytics, relocation_profile, the policy_*
# family, recommendations, and the relocation routes). Removing them tightens the
# guard so it now catches a regression on any of those 18, instead of silently
# grandfathering them. Verified via `python3 scripts/check_router_registrations.py`
# (AST diff): exactly these 3 identities remain modular-only on main.
#
# Remaining triage (all OFF the MVP customer critical path — DE/FR/GB HR+employee flow):
#   routers.ab_tests.router — /api/ab-tests/promote,/rollback. Reachable from the
#     ADMIN-only AdminAbTestsPage (/admin/ab-tests); these two POSTs 405 in prod.
#     Admin-internal, not customer-facing. Register in backend/main.py when the
#     admin A/B dashboard is exercised.
#   routers.pets.router — /api/cases/{id}/pets CRUD. frontend/src/api/pets.ts exists
#     but is imported by no page today, so the 405 is latent (no live caller).
#   routers.support.router — /api/support/ticket,/reply,/triage,/webhooks/support-email.
#     No frontend caller; support is manual (Romain) for MVP. Latent.
routers.ab_tests.router
routers.pets.router
routers.support.router
