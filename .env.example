# Frontend (Vite) — must be prefixed with VITE_
VITE_API_URL=http://localhost:8000

# Backend (FastAPI) — set in your shell or Render env vars
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
