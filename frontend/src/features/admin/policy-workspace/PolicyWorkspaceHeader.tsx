/** Single source for Policy Workspace page copy (used with AdminLayout). */
export const POLICY_WORKSPACE_TITLE = 'Policy Workspace';

export const POLICY_WORKSPACE_SUBTITLE =
  'Define the structured relocation policy baseline for this company. This workspace controls what support is included, excluded, or capped across the relocation lifecycle.';
