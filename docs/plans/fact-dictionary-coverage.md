# Fact-dictionary coverage & drift audit

_Generated by `scripts/fact_dictionary_coverage.py` — read-only diagnostic for Build B of [the form-fill integration plan](form-fill-integration-build-plan.md)._

## Registry state

- Governed facts: **18** (5 consult-professional / never-filled).
- `prefill_source` prefixes in use: {'profile': 5, 'contract': 5, 'case': 2} (+ 6 with no source).

## 1. Form-field coverage

Real form fields in `form_field_mappings`: **15** distinct vault paths. Governed by a fact today: **7**. Not yet governed: **8**.

| vault_field_path | status |
|---|---|
| `gender` | ❌ no governing FactEntry — a form using it can't resolve a value |
| `legal_first_name` | ❌ no governing FactEntry — a form using it can't resolve a value |
| `legal_last_name` | ❌ no governing FactEntry — a form using it can't resolve a value |
| `marital_status` | ❌ no governing FactEntry — a form using it can't resolve a value |
| `passport_country` | ❌ no governing FactEntry — a form using it can't resolve a value |
| `passport_issue_date` | ❌ no governing FactEntry — a form using it can't resolve a value |
| `place_of_birth` | ❌ no governing FactEntry — a form using it can't resolve a value |
| `salary_amount` | ❌ no governing FactEntry — a form using it can't resolve a value |

## 2. Join-key drift (integration hazard #1)

The registry names a fact one way; the form mapping names the same concept another. Until reconciled, a value captured once will **not** fill the other form.

| form mapping vault path | nearest registry source key | note |
|---|---|---|
| `legal_first_name` | `legal_full_name` | reconcile onto one canonical key |
| `legal_last_name` | `legal_full_name` | reconcile onto one canonical key |
| `passport_country` | `passport_expiry, passport_number` | reconcile onto one canonical key |
| `passport_issue_date` | `passport_expiry, passport_number` | reconcile onto one canonical key |
| `salary_amount` | `salary_amount_nok` | reconcile onto one canonical key |

> Also note the **prefix mismatch**: the registry resolves via `prefill_source` prefixes (`profile.`/`contract.`/`case.`) while `form_field_mappings.vault_field_path` are bare `imm_employee_profiles` columns. The prefill ladder must map one to the other, or the join breaks.

## 3. Consult-professional firewall — demand vs. coverage

Corridor-content carries **259** consult-professional steps across **32** corridors (of 580 total records). The registry firewalls **5** facts today (tax_residency_status, shadow_payroll_requirement, pe_risk, a1_determination, contract_classification).

These are different layers — corridor-content flags *steps*, the registry flags *fields* — but the firewall must be enforced wherever a regulated field is auto-fillable. As field facts are added for new corridors, each regulated one must inherit `professional_review_required=True`.

## Next actions

1. Reconcile the join key: one canonical name per fact, mapped to both the prefill `prefill_source` and the `vault_field_path` (close §2).

2. Add FactEntry rows for the 8 uncovered form fields (close §1).

3. Keep this audit green in CI so the dictionary can't silently drift as corridors are added.

