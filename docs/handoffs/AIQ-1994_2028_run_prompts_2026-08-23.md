# AIQ-1994 → AIQ-2028 — the two prompts you need to run

**Status as of 2026-08-23: nobody has produced the material.**

AIQ-1994 is marked `Done`, but its `OUTCOME: code` was PR #1939 (the source-host allowlist fix),
not the research. Its own Next steps say step 1 is *"Romain: paste the card into Audos General
Chat."* That has not happened, so `docs/imports/ie-eu-eea-freemover-2026-08-22/` contains only the
read-only context bundle and AIQ-2028 is `Blocked`.

Prerequisite already satisfied: PR #1939 is on `main`, so `hse.ie`, `rtb.ie`, `ndls.ie`, `rsa.ie`,
`welfare.ie` and `mywelfare.ie` now pass `classify_source()`. Without it most of this batch would
have been rejected at import as UNOFFICIAL.

---

## PROMPT 1 — for Otto, in Audos General Chat

Source of truth: `audos-workspace-776786/prompts/AIQ-1994-ireland-eu-free-mover-card.md`
(unchanged, reproduced here so you can paste without opening the file).

**Two mechanical warnings, both learned the hard way:**
- Paste it as **one single block**. A newline in the Audos composer submits the message.
- **Do not attach it as a file** — attachments come back empty.

```text
Research what a citizen of an EU/EEA member state must do on arrival in Ireland to take up employment. Worked example: a Spanish national moving Madrid to Dublin. The audience is exclusively EU/EEA free movers. Ireland's non-EEA visa, employment-permit and IRP tracks are already covered and must not appear.

ALREADY COVERED — do not research these, we already hold them, anything you produce on them is a duplicate: registering the job with Revenue / myAccount, the Revenue Payroll Notification (RPN), emergency tax, PRSI compulsion, combining social insurance contributions paid abroad, the 183/280-day tax residence test, and split-year treatment.

WHAT IS MISSING is everything upstream of payroll — above all the PPSN, which every one of those covered facts silently depends on.

Write the deliverable to audos-workspace-776786/data/IE-eu-immig-2026-08-21.jsonl, one JSON object per line. Follow the shape of ve-ie-entry-family-2026-08-20.jsonl exactly. Set destination_country to "IE". The applies_to object must contain: "corridor": "ES->IE", "nationality": "EU", "status": "professional", "fact_uid": "ES_IE:EU_EEA:<entity_topic_key>", "pillar", "non_obvious", "needs_lawyer_review", "quote_verbatim_confirmed", "source_name", and "batch_id": "IE-eu-immig-2026-08-21".

RULES, all mandatory:

1. "nationality": "EU" and "status": "professional" on every single line. Never "any" — a rule that applies to everyone still applies to EU nationals, so scope it EU. A missing nationality makes the entity unusable; a missing status makes the row invisible to the employment track. Both fail silently, so omit neither.

2. evidence_quote must be a verbatim sentence copied from the page at source_url, not a paraphrase. A fact you cannot quote is a fact you must drop.

3. Set "quote_verbatim_confirmed": true only if you re-read the quote against the live page. Otherwise false. Do not omit the key — false is honest and costs only a review flag, whereas a wrong true badges an unchecked claim as verified.

4. source_url must be on one of: gov.ie, irishimmigration.ie, hse.ie, rtb.ie, ndls.ie, rsa.ie, welfare.ie, mywelfare.ie (preferred — these are the publishing authority), or citizensinformation.ie, revenue.ie (acceptable only where nothing else publishes the rule plainly). Any other domain is rejected on import. Mixing a weaker source into a topic downgrades that whole requirement, so keep each topic on its best available source.

5. fact_type is one of: eligibility, step, document, deadline, fee, where_to_apply, other. confidence is one of: high, medium, low.

6. One entity_topic_key per requirement. entity_title is what the relocating employee reads — a real imperative phrase such as "Get a Personal Public Service Number (PPSN)", never a humanised topic key like "Ireland — ppsn registration".

7. Keep the fact_text house style: "Commonly believed: ... / Actually: ... / Action required: ..."

DO NOT INVENT A REQUIREMENT THAT DOES NOT EXIST. Ireland has no address registration, no padron, no residence-permit card and no immigration registration for EEA nationals. Do not produce facts for those merely because France, Germany or Spain have them. Where the honest answer is "no such obligation exists for an EU national", put that in your chat reply, not in the file.

TOPICS to confirm or drop, each with evidence — and add any I have missed: ppsn_registration (highest priority, the precondition for everything else); eu_right_of_residence (the right to reside beyond three months as a worker, and the fact that no registration or card is required); travel_id_document (national ID card or passport — Ireland is outside Schengen and the Common Travel Area does not cover Spain); public_health_entitlement (ordinary-residence basis, EHIC in the interim, GP visit card, medical card); rtb_tenancy_registration (what a tenant should expect and verify); driving_licence_exchange (validity of a Spanish licence in Ireland, and whether exchange is required or optional).

When the file is written, reply with the total line count and each entity_topic_key with its fact count and source host.
```

**What you should get back:** a chat reply giving the total line count plus each
`entity_topic_key` with its fact count and source host. Keep that reply — step 2 reconciles
against it.

---

## PROMPT 2 — for Claude Code, once `[audos-sync]` has committed the JSONL

Do not run this until the file exists in the repo. `[audos-sync]` commits it; if it has not
appeared, the batch has not been delivered and there is nothing to import.

```text
AIQ-2028 — load the Ireland EU/EEA free-mover batch as candidates.

Batch id: IE-eu-immig-2026-08-21
File: audos-workspace-776786/data/IE-eu-immig-2026-08-21.jsonl

Before importing anything:
1. Confirm the file exists and is tracked, and print its line count. If it is absent, STOP and
   report that the batch was not delivered — do not reconstruct it.
2. Reconcile that line count against the count Otto reported in chat (I will paste it). A
   discrepancy is the batch's problem to explain, not yours to reconcile away.
3. Verify the target table and key against the LIVE schema before writing a loader. Corridor
   requirement data lands in public.requirement_items, whose varchar id carries the batch's own
   uid; requirement_facts is keyed differently. A manifest naming a table is a claim, not a schema.

Then:
4. Dry run first — it is the default:
     ./.venv311/bin/python scripts/import_otto_facts.py IE-eu-immig-2026-08-21
   Report the parse/reject breakdown. If rejects cluster by host, suspect the source allowlist in
   backend/imports/otto/parsers.py before suspecting the research — that allowlist has been too
   narrow three times.
5. Only if the dry run is clean:
     ./.venv311/bin/python scripts/import_otto_facts.py IE-eu-immig-2026-08-21 \
       --promote --source-label "Ireland EU Free Movement Research" --expected <N>
   where <N> is the reconciled count from step 2.

Hard rules:
- Candidates only. status='pending' / review_status='pending'. NEVER approved, verified or live.
  review_status DEFAULTS to 'approved' on requirement_items — set 'pending' explicitly, and never
  let an ON CONFLICT update overwrite a reviewer's decision.
- Never fill a gap. A field the artifact does not carry stays NULL. A record with
  source_missing=true keeps the flag and gets no invented citation.
- Watch for two known importer defects and report whether they bit this batch:
  AIQ-2035 — a batch with no applies_to.status files every row at purpose='other', invisible to
  the employment track. AIQ-2036 — mappings.py ignores applies_to.pillar even though Otto emits
  it, so a manual re-pillar is needed after promote.
- Re-staging cannot update applies_to (ON CONFLICT DO NOTHING), so get it right on the first
  promote or the rows must be deleted and re-staged.

Finally, report: rows staged, rows promoted, rows rejected with reasons, and the three things a
human must still do — re-pillar, raise travel_id_document to BLOCKER, and flip review_status to
approved (the sole publication gate, crud.list_requirements).
```

---

## Also worth knowing

**AIQ-1994 is marked `Done` but its Expected Output was never delivered.** Its Expected Output is
the NDJSON; what shipped was the allowlist fix. That is the phantom-Done pattern the queue-hygiene
rules exist to prevent — a ticket whose only artifact is adjacent code reads as finished in the log.
Consider reopening it, or splitting the research half into its own card, so AIQ-2028's blocker is
visible on the board rather than only in this document.
