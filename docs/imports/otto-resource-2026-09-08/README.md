# otto-resource-2026-09-08 — DISPATCHED to Otto (awaiting delivery)

**Status: awaiting Otto's GCS deliverable.** This is a research handoff, not a landed batch.

The "verifiable held facts" tier of `HELD_FACTS_WORKLIST.md` is cleared (CO/PA/HK/AT/JP + Denmark).
The remaining Tier-2 items have prod coverage already and their missing facts were folded into
researcher notes — they need **re-research from scratch**, so per the founder's steer this batch was
routed to Otto (the dedicated research lane) rather than in-process subagents.

## Dispatch
- Bridge: `POST audos.com/api/hooks/execute/workspace-776786/otto-claude-bridge`
  `{action:"push", direction:"claude_to_otto", kind:"message", source:"claude-code", payload:{…}}`
- Accepted **id 21**, 2026-09-08.
- Secret in `~/.otto-bridge-secret` (x-hook-secret header only — never in the payload/logs).

## Targets (5 countries, 8 facts)
| ISO | Fact | Source hint |
|---|---|---|
| IN | PAN mandatory — Income-Tax Act §139A | incometaxindia.gov.in / indiacode.nic.in (403 to plain fetch) |
| BR | VITEM V needs prior residence authorization (MJ/CNIg) before consular issue | gov.br/mre, gov.br/mj CNIg resolution (CAPTCHA) |
| IL | Foreign-expert tax regime (exact rule + rate) | taxes.gov.il / Income Tax Ordinance |
| PT | EU Blue Card intra-EU mobility deadline (~30 days) | AIMA / Law 23/2007 art. 61-A on dre.pt |
| EE | 4 facts — labour-market test; permit-card-as-ID; family-doctor choice; driving-licence exchange window | eesti.ee (JS SPA), politsei.ee, transpordiamet.ee, riigiteataja.ee |

Full brief (record contract + hard rules) sent in the bridge payload; regenerated by
`scratchpad/otto_brief.py`.

## When Otto delivers (procedure for the next session)
1. Pull the bus: `{action:"pull", direction:"otto_to_claude"}` — read the manifest GCS URL from Otto's reply.
2. Fetch the manifest first; reconcile counts + sha256 against it exactly.
3. **Verify every quote independently** (browser-ground JS-SPA/PDF sources, `confirm_quotes.py` the rest)
   — Otto's `quote_verbatim_confirmed:true` is not sufficient; the Indonesia hold this session was a
   researcher quote that was visually "confirmed" but wrong.
4. Land ONLY what verifies, via the normal pipeline (import → `promote(country=<ISO>)`), append-only
   (approved count unchanged, expert_verified 0, scope guard green). Re-scope any EEA
   registration/permit rows to `["EU_EEA"]`. Hold anything that doesn't verify with an honest note.
5. Blocked facts Otto returns with a `blocked_reason` stay held on the worklist — do not invent a source.
